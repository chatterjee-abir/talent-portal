import React from 'react';
import ReactDOM from 'react-dom/client';
import TalentPortal from './TalentPortal';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<TalentPortal />);
